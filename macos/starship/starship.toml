format = '$all$username[::](bold blue)$hostname$character'

[kubernetes]
format = '[$symbol$context|$namespace]($style) '
disabled = false
context_aliases = { "admin@staging" = "staging" }
style = "blue bold"

[username]
disabled = false
show_always = true
format = '[$user]($style)'

[hostname]
ssh_only = false
ssh_symbol = ' 🌐'
format = '[$hostname](bold purple)[$ssh_symbol](bold blue)'
trim_at = '.local'
disabled = false